# playwright is pinned to the base image's tag (build.yaml) so the pip package
# and the bundled Chromium revision match. We install it explicitly (rather than
# relying on the image's preinstalled copy) because that copy lives in a
# different interpreter than the one run.sh launches on Ubuntu noble - installing
# via `python3 -m pip` puts it where the app's `python3` will find it.
playwright==1.49.1
# 3.0.5 = the maintained fork Home Assistant's own samsungtv integration uses;
# 2.6.0's art protocol fails on 2022+ Frames (ms.channel.timeOut on upload).
samsungtvws==3.0.5
websocket-client>=1.6,<2     # sync transport used by samsungtvws
Pillow>=10.0
requests>=2.31
astral>=3.2     # sun elevation for the circadian background
